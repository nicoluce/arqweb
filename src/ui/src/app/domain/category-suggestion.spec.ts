import { CategorySuggestion } from './category-suggestion';

describe('CategorySuggestion', () => {
  it('should create an instance', () => {
    expect(new CategorySuggestion()).toBeTruthy();
  });
});
